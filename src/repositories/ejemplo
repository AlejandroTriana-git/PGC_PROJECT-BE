//Se encarga de todo el SQL, acceso a los datos:


//Example
const findByEmail = async (email) => {
    const [rows] = await pool.query(
        'SELECT * FROM users WHERE email = ?',
        [email]
    );

    return rows[0];
};